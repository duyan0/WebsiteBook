@model BanSach.Models.DanhMuc_TheLoai

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>DanhMuc_TheLoai</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ID)

        <div class="form-group">
            @Html.LabelFor(model => model.DanhMuc_ID, "Khuyến mãi", new { @class = "control-label font-weight-bold text-dark" })
            @Html.DropDownListFor(model => model.DanhMuc_ID, (SelectList)ViewBag.DM, "Chọn mức giảm giá", new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.DanhMuc_ID, "", new { @class = "text-danger" })
        </div>
        <div class="form-group">
            @Html.LabelFor(model => model.TheLoai_ID, "Khuyến mãi", new { @class = "control-label font-weight-bold text-dark" })
            @Html.DropDownListFor(model => model.TheLoai_ID, (SelectList)ViewBag.TL, "Chọn mức giảm giá", new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.TheLoai_ID, "", new { @class = "text-danger" })
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.HinhAnh, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <input type="file" name="HinhAnh" class="form-control" />
                @Html.ValidationMessageFor(model => model.HinhAnh, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
