@model BanSach.Models.DanhMuc

@{
    ViewBag.Title = "Xoá danh mục";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}



<div class="container mt-4">
    <h2 class="text-danger text-center">Xoá danh mục</h2>

    <hr />
    <table class="table table-bordered">
        <thead class="thead-dark">
            <tr>
                <th>Thông tin</th>
                <th>Nội dung</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><b>ID</b></td>
                <td>@Html.DisplayFor(model => model.TenDanhMuc)</td>
            </tr>
        </tbody>
    </table>

    @if (ViewBag.ErrorMessage != null)
    {
        <div class="alert alert-danger mt-4 rounded-sm">
            @ViewBag.ErrorMessage
        </div>
    }

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions">
            @Html.ActionLink("Quay về quản lý danh mục", "Index", null, new { @class = "btn btn-secondary me-2 rounded-sm" })
            <input type="submit" value="Xoá" class="btn btn-danger rounded-sm" />
        </div>

    }
</div>

