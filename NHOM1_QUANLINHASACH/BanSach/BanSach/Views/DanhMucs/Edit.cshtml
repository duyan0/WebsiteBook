@model BanSach.Models.DanhMuc

@{
    ViewBag.Title = "Chỉnh sửa danh mục";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card shadow-sm border-0 rounded-lg">
                <div class="card-header text-center text-white">
                    <h3 class="mb-2 fw-semibold text-dark ">Chỉnh sửa thể loại - danh mục</h3>
                </div>
                <div class="card-body">
                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                        @Html.HiddenFor(model => model.ID)
                        <div class="form-group row mb-4">
                            @Html.LabelFor(model => model.TenDanhMuc, "Tên danh mục", htmlAttributes: new { @class = "col-md-3 col-form-label font-weight-bold" })
                            <div class="col-md-9">

                                <input type="text" class="form-control" id="TenDanhMuc" name="TenDanhMuc" value="@Model.TenDanhMuc" />
                                @Html.ValidationMessageFor(model => model.TenDanhMuc, "", new { @class = "text-danger" })
                            </div>
                        </div>

                        <div class="form-group text-center">
                            <button type="submit" class="btn btn-success  px-5">
                                Lưu
                            </button>
                            @Html.ActionLink("Quay về ", "Index", null, new { @class = "btn btn-secondary" })
                           
                        </div>
                    }

                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
